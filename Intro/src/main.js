"use strict";
// const getFullName = (name: string, surname: string): string => {
//   return `${name} ${surname}`;
// };
// console.log(getFullName("Yuki", "Bunny"));
const user = {
    name: "Yuki",
    age: 20,
};
const user2 = {
    name: "Barbaloot",
    age: 21,
};
