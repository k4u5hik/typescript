// const getFullName = (name: string, surname: string): string => {
//   return `${name} ${surname}`;
// };
// console.log(getFullName("Yuki", "Bunny"));

interface User {
  name: string;
  age: number;
}

const user: User = {
  name: "Yuki",
  age: 20,
};
const user2: User = {
  name: "Barbaloot",
  age: 21,
};
